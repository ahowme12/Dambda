name: Deploy Frontend to S3

on:
  push:
    branches:
      - main
    paths:
      - "app/**"
      - ".github/workflows/deploy-frontend.yml"

  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: frontend-production
  cancel-in-progress: true

env:
  AWS_REGION: ap-northeast-2
  S3_BUCKET: my-app-dev-static-site-942679463970
  # API Gateway가 재생성되면(엔드포인트 서브도메인이 랜덤이라) 이 값도 같이 갱신해야 함
  API_BASE_URL: https://taqs2nhy9f.execute-api.ap-northeast-2.amazonaws.com

jobs:
  deploy:
    name: Build and sync
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        working-directory: app
        run: flutter pub get

      - name: Build web
        working-directory: app
        run: flutter build web --release --dart-define=API_BASE_URL=${{ env.API_BASE_URL }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-skip-session-tagging: true

      - name: Sync to S3
        run: |
          aws s3 sync app/build/web "s3://$S3_BUCKET" --delete
